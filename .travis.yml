sudo: required

language: python

services:
  - docker

before_install:
  - docker build -t test_ngraph_onnx .ci/travis/ubuntu

install: true

script:
  - docker run test_ngraph_onnx

branches:
  only:
    - "master"
